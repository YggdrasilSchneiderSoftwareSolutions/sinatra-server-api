# sinatra-server-api
A small api service which returns server information and other stuff to display on a dashboard.
Althought this repo contains a Dockerfile you shouldn't run in a container since you actually want the values from
the host system.
